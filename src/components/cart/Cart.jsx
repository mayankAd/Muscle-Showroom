import React from 'react'
import styles from './Cart.module.css'

const Cart = (props) => {
  return (
    <div className={`${styles.container}`}>
        <button>Cart</button>
        </div>
  )
}

export default Cart